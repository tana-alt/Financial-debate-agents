schema_version: "0.1"
record_type: verification_record
status: draft

identity:
  verification_id: example_verification_id
  work_id: example_work_id
  project_id: example_project
  created_at: "YYYY-MM-DD"

checks:
  - name: "foundation robustness gate"
    command: "make check-foundation"
    result: passed
    evidence_ref: "artifact/example-evidence.md"
    notes: "Required local chain plus CD readiness guard."
  - name: "lint"
    command: "make lint"
    result: skipped
    evidence_ref: "none"
    notes: "Skipped in this template example; record the reason when skipped."
  - name: "test"
    command: "make test"
    result: skipped
    evidence_ref: "none"
    notes: "Skipped in this template example; record the reason when skipped."

unverified_surfaces:
  - "none"
residual_risk:
  - "none"
human_gate:
  required: false
  status: not_applicable
  reason: "template example"
next_action: complete
