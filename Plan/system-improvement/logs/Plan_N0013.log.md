---
plan_id: Plan_N0013
project_id: system-improvement
plan_ref: Plan/system-improvement/plans/Plan_N0013.md
---

# Plan_N0013 Log

## 2026-06-01 Kickoff

- User asked to revise the execution Plan after sub-agent review.
- Kept durable planning output under `Plan/system-improvement/`.
- Did not create any `artifact/` output.

## 2026-06-01 Review Inputs

- System-design reviewer returned `REWORK` for the earlier draft because
  guidance detection needed explicit propagation into workflow state, routing,
  inspect output, and report-quality behavior.
- Provider-spec reviewer returned `PASS` for adding provider-side structured
  output with Pydantic validation and retry fallback retained.
- Parent confirmed local SDK availability:
  - `openai 2.38.0`
  - `anthropic 0.105.2`
  - `OpenAI.responses.parse`: present
  - `OpenAI.chat.completions.parse`: present
  - `Anthropic.messages.parse`: present

## 2026-06-01 Plan Revision

- Added `Plan_N0013` as a follow-up draft instead of editing completed
  `Plan_N0012`.
- Reframed guidance detection as source routing and missing-data accounting.
- Added `IngestedReviewInput`, `GuidanceFact`, `RoutingReport`, and
  `ContextBudgetReport` target contracts.
- Required `GuidanceFact.source_refs` or candidate section IDs to drive
  `GuidanceAnalyst` routing.
- Required a no-LLM `inspect-input` CLI before real provider calls.
- Required provider-side structured output where supported, while retaining
  Pydantic validation and current fallback behavior.

## 2026-06-01 Verification

- `python - <<'PY' ...` parsed `Plan/system-improvement/index.yaml` and
  confirmed `Plan_N0013` has existing plan/log paths: passed.
- `rg -n "[[:blank:]]$"` across `Plan_N0013` plan, log, and index returned no
  matches: passed.

## 2026-06-01 Execution Slice 1

- Scope: guidance classification/routing and input inspection surfaces only.
- Replaced default workflow guidance ingestion gate with non-throwing
  classification and retained strict guidance as an explicit opt-in.
- Extended `GuidanceFact` with status, confidence, source refs, candidate
  section IDs, matched terms, signal strength, and reason.
- Routed `GuidanceAnalyst` from classified source refs/section IDs before
  heading heuristics so generic PDF/text headings can still route body guidance.
- Added deterministic missing-guidance behavior for `not_found` so the workflow
  does not ask `GuidanceAnalyst` to fabricate evidence.
- Added `earnings-debate inspect-input` audit output for normalized input,
  metrics, source manifest, section preview, guidance, routing, and context
  budget without LLM calls.

### Verification

- `uv run --active pytest tests/test_report_quality_full.py tests/test_cli_smoke.py tests/test_workflow_api.py tests/test_workflow_e2e.py`: passed, 25 tests.
- `uv run --active ruff check src/report_quality_guidance.py src/workflow_models.py src/workflow.py src/main.py tests/test_report_quality_full.py tests/test_workflow_api.py tests/test_cli_smoke.py`: passed.

## 2026-06-01 Execution Start

- Split execution into:
  - sub-agent execution slice for guidance classification, guidance routing,
    inspect-input, and budget audit;
  - parent execution slice for provider-side structured output.
- Real LLM API calls remain gated for user-attended execution.

## 2026-06-01 Parent Structured Output Slice

- Added a provider-compatible structured completion boundary:
  - OpenAI uses SDK structured parse when available.
  - Anthropic uses SDK structured parse when available.
  - unsupported structured parse falls back to existing text completion.
- Updated workflow agents to use structured completion when supported and keep
  compatibility with existing test LLMs that expose only `complete()`.
- Kept Pydantic parsing and role/source validation after provider output.
- Added normal `run` context-budget enforcement before provider calls, using
  the same budget report generated by `inspect-input`.
- Focused verification:
  - `uv run --active pytest -q tests/test_report_quality_full.py tests/test_cli_smoke.py tests/test_workflow_api.py tests/test_workflow_e2e.py tests/test_llm_provider.py tests/test_workflow_agents.py`:
    passed, `43 passed`, `1 warning`.
  - `uv run --active ruff format --check src/llm.py src/main.py src/report_quality_guidance.py src/workflow.py src/workflow_agents.py src/workflow_models.py tests/test_cli_smoke.py tests/test_llm_provider.py tests/test_report_quality_full.py tests/test_workflow_api.py`:
    passed.
  - `uv run --active ruff check src/llm.py src/main.py src/report_quality_guidance.py src/workflow.py src/workflow_agents.py src/workflow_models.py tests/test_cli_smoke.py tests/test_llm_provider.py tests/test_report_quality_full.py tests/test_workflow_api.py`:
    passed.

## 2026-06-01 Current Samples And Local Smoke

- Added current request examples:
  - `samples/request.current.local-presentation.example.json`
  - `samples/request.current.yfinance-presentation.example.json`
  - `samples/request.current.sec-fallback.example.json`
  - `samples/request.current.guidance-missing.example.json`
- Updated `tests/fixtures/sample_presentation.txt` with an explicit financial
  outlook/guidance line so local document ingestion can exercise guidance
  routing.
- Generated tracked fake-provider example output under
  `outputs/example-current/`.
- Generated ignored local inspect output under `outputs/input-audit-current/`.
- Local smoke:
  - `uv run --active earnings-debate inspect-input --input-json samples/request.current.local-presentation.example.json --out outputs/input-audit-current`:
    passed.
  - `LLM_PROVIDER=fake uv run --active earnings-debate run --api-url local --input-json samples/request.current.local-presentation.example.json --out outputs/example-current`:
    passed.
  - `LLM_PROVIDER=fake uv run --active earnings-debate run --api-url local --input-json samples/request.current.guidance-missing.example.json --out /tmp/earnings-guidance-missing-smoke`:
    passed.

## 2026-06-01 Final Local Verification

- Cross-review sub-agent returned `PASS`; no blocking findings.
- `uv run --active pytest -q`: passed, `150 passed`, `1 warning`.
- `uv run --active ruff format --check .`: passed.
- `uv run --active ruff check .`: passed.
- `git diff --check`: passed.
- Real LLM/API provider smoke was not run; it remains gated for user-attended
  execution.

## 2026-06-01 Non-LLM Real Input Validation

- User allowed external execution except LLM provider calls.
- Execution sub-agent ran `inspect-input` only; no real LLM provider calls.
- `samples/request.current.yfinance-presentation.example.json`: passed.
  - Output: `/tmp/earnings-debate-inspect-N0013/yfinance-presentation`.
  - Metrics present, `1` section, guidance `found`, `GuidanceAnalyst` routed
    `1` section, all context budgets passed.
- `samples/request.current.sec-fallback.example.json`: initially failed because
  the SEC filing URL used accession `000104581025000023` with
  `nvda-20250427.htm`, returning `404`.
- Corrected the SEC fallback sample file to accession `000104581025000116`.
- Parent re-ran the corrected SEC sample:
  - `SEC_USER_AGENT=... uv run --active earnings-debate inspect-input --input-json samples/request.current.sec-fallback.example.json --out /tmp/earnings-debate-inspect-N0013-parent-sec`:
    passed.
  - Sections: `4`.
  - Guidance status: `ambiguous`.
  - Matched guidance signal: weak `forward-looking statements`.
  - `GuidanceAnalyst` routed `1` section by `guidance_fact`.
  - All context budgets passed.
  - Normalized yfinance metrics included revenue, operating cash flow, free cash
    flow, capex, and EPS consensus; actual EPS was absent from yfinance.
- Execution sub-agent downloaded a real NVIDIA presentation PDF into ignored
  local `data/` and ran `inspect-input`.
  - PDF: `data/NVDA-F1Q27-Quarterly-Presentation-FINAL.pdf`.
  - Output: `/tmp/earnings-debate-inspect-N0013/nvda-f1q27-pdf`.
  - Sections: `16`.
  - Guidance status: `found`.
  - `GuidanceAnalyst` routed `3` sections.
  - All context budgets passed.
- Raw PDF, SEC cache, and inspect outputs remain ignored/local and are not
  intended for tracked repo truth.
- Cross-review sub-agent returned `PASS`.
  - Confirmed no LLM provider calls were made.
  - Confirmed corrected SEC URL returns HTTP `200`.
  - Confirmed yfinance+presentation, SEC fallback, and real PDF inspect
    summaries are meaningful.
  - Confirmed raw PDF, SEC cache, and inspect outputs are ignored/untracked.

## 2026-06-01 Expanded CLI Policy Validation

- User added real PDFs for explicit outlook and no-guidance/ambiguous cases.
- Parent ran `inspect-input` against six non-LLM inputs:
  - `GOOGL 2026Q1` PDF: `data/Alphabet-Q1-2026-Earnings-Slides.pdf`.
  - `MSFT 2026Q2` PDF: `data/msft-q2-outlook.pdf`.
  - `NVDA 2027Q1` PDF:
    `data/NVDA-F1Q27-Quarterly-Presentation-FINAL.pdf`.
  - `GOOGL 2026Q1` SEC 10-Q:
    `https://www.sec.gov/Archives/edgar/data/1652044/000165204426000048/goog-20260331.htm`.
  - `MSFT 2026Q2` SEC 10-Q:
    `https://www.sec.gov/Archives/edgar/data/789019/000119312526191507/msft-20260331.htm`.
  - `NVDA 2027Q1` SEC 10-Q:
    `https://www.sec.gov/Archives/edgar/data/1045810/000104581026000052/nvda-20260426.htm`.
- PDF results:
  - `GOOGL`: `11` sections, guidance `ambiguous`, weak
    `forward-looking statements` match, `GuidanceAnalyst` routed `1` section,
    all context budgets passed.
  - `MSFT`: `1` section, guidance `found`, strong `outlook` match,
    `GuidanceAnalyst` routed `1` section, all context budgets passed.
  - `NVDA`: `16` sections, guidance `found`, strong `business outlook`,
    `outlook`, and `full year` matches, `GuidanceAnalyst` routed `3`
    sections, all context budgets passed.
- SEC fallback results:
  - `GOOGL`: `5` sections, guidance `found`, strong `guidance` match,
    `GuidanceAnalyst` routed `filing:guidance`, all context budgets passed.
  - `MSFT`: `6` sections, guidance `found`, strong `guidance`,
    `business outlook`, and `outlook` matches, `GuidanceAnalyst` routed
    `filing:eps` and `filing:guidance`, all context budgets passed.
  - `NVDA`: `4` sections, guidance `ambiguous`, weak
    `forward-looking statements` match, `GuidanceAnalyst` routed
    `filing:risk`, all context budgets passed.
- Normalized yfinance metrics were present for revenue, operating cash flow,
  free cash flow, capex, and EPS consensus in all six cases. Actual EPS and
  revenue consensus were absent in these observed yfinance snapshots.
- Ran fake-provider CLI `run` for all six inputs. All completed with
  `verdict=good`, `confidence=0.65`, `7` workflow steps, and generated
  non-empty `report.md` plus `workflow_result.json`.
- SEC filing ingestion emitted `XMLParsedAsHTMLWarning` for the tested 10-Q
  HTML/XML filings. This did not block processing, but it supports treating SEC
  segmentation as heuristic fallback evidence rather than the primary guidance
  source when a company earnings PDF is available.
- Operational policy supported by this run:
  - Prefer company PDF/presentation as primary source when available,
    especially for guidance/outlook routing.
  - Use yfinance as a normalized financial metrics source with explicit
    missingness caveats; do not rely on it alone for actual EPS or revenue
    consensus/surprise.
  - Use SEC filing as fallback or supplement for official filing text, risk,
    MD&A, and financial sections, with guidance classification treated more
    conservatively when only weak forward-looking language is found.
  - Do not stop by default when guidance is `ambiguous` or `not_found`; surface
    it in the report unless strict guidance is explicitly enabled.
- Cross-review sub-agent returned `PASS with CONCERNS`.
  - Policy evidence was confirmed across the six inspect/fake-run cases.
  - SEC guidance classification can still false-positive on non-business
    wording such as accounting/tax/FASB guidance.
  - Weak forward-looking disclaimer matches should not be presented as
    confirmed business guidance in final reports.
  - Fake-provider report did not surface observed missing metrics strongly
    enough; report/prompt contract should require missingness caveats before
    real LLM provider evaluation.
  - CLI parser warnings are visible in command output but are not persisted in
    inspection artifacts.
